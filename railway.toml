[build]
builder = "nixpacks"

[build.nixpacks.providers]
node = true
python = true

[build.steps]
install = "npm install --prefix frontend && pip install -r requirements.txt"
build = "npm run build --prefix frontend"

[deploy]
startCommand = "python -m uvicorn api:app --host 0.0.0.0 --port $PORT"
